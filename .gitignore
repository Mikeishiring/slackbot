node_modules/
# The family, not one filename — .env.local / .env.production / .env.backup
# all carry the same three live credentials.
.env*
!.env.example
dist/
.claude/

# Throwaway probe scripts from analysis agents
.probe-*
